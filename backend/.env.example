# Server
PORT=4000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Database
MONGODB_URI=mongodb://127.0.0.1:27017/jiva-space

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Admin seed endpoint (POST /api/admin/seed with header x-seed-secret)
SEED_SECRET=change-me-to-a-random-string
